//------------------------------------------------------------------------------
// <auto-generated>
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
using System;
using System.Collections.Generic;
using System.Text;
using NHibernate;
using NHibernate.Context;
using NUnit.Framework;
using StructureMap;
using rt.core.business.nhibernate;
using rt.core.business.registry;
using rt.core.model;
using rt.fias.business.manager;
using rt.fias.model.fias;

namespace rt.fias.business.tests
{
	[TestFixture]
    public partial class AddressObjectTypeTests : UnitTestbase
    {
        [SetUp]
        public void SetUp()
        {
            Bootstrapper.Bootstrap();
            session = ObjectFactory.GetInstance<ISessionFactory>().OpenSession();
            CurrentSessionContext.Bind(session);
            manager = ObjectFactory.GetInstance<IAddressObjectTypeManager>();
            manager.Session.BeginTransaction();
        }

        [TearDown]
        public void TearDown()
        {
            manager.Session.RollbackTransaction();
        }
        
        protected rt.fias.business.manager.IAddressObjectTypeManager manager;
        
        protected ISession session { get; set; }
		
		public static AddressObjectType CreateNew (int depth = 0)
		{
			rt.fias.model.fias.AddressObjectType entity = new rt.fias.model.fias.AddressObjectType();
			
			// You may need to maually enter this key if there is a constraint violation.
			entity.Id = "Tes";
			
      entity.Level = 34;
      entity.Scname = "T";
      entity.Socrname = "Test Test Test Tes";
			
			return entity;
		}
		protected rt.fias.model.fias.AddressObjectType GetFirstAddressObjectType()
        {
            IList<rt.fias.model.fias.AddressObjectType> entityList = manager.GetAll(1);
            if (entityList.Count == 0)
                Assert.Fail("All tables must have at least one row for unit tests to succeed.");
            return entityList[0];
        }
		
		[Test]
        public void Create()
        {
            try
            {
				rt.fias.model.fias.AddressObjectType entity = CreateNew();
				
                object result = manager.Save(entity);

                Assert.IsNotNull(result);
            }
            catch(Exception ex)
            {
                Assert.Fail(ex.ToString());
            }
        }
        [Test]
        public void Read()
        {
            try
            {
                rt.fias.model.fias.AddressObjectType entityA = CreateNew();
				manager.Save(entityA);

                rt.fias.model.fias.AddressObjectType entityB = manager.GetById(entityA.Id);

                Assert.AreEqual(entityA, entityB);
            }
            catch (Exception ex)
            {
                Assert.Fail(ex.ToString());
            }
        }
		[Test]
		public void Update()
        {
            try
            {
				rt.fias.model.fias.AddressObjectType entityC = CreateNew();
				manager.Save(entityC);
				manager.Session.GetISession().Flush();
				manager.Session.GetISession().Clear();
			
                rt.fias.model.fias.AddressObjectType entityA = GetFirstAddressObjectType();
				
				entityA.Level = 80;
				
				manager.Update(entityA);

                rt.fias.model.fias.AddressObjectType entityB = manager.GetById(entityA.Id);

                Assert.AreEqual(entityA.Level, entityB.Level);
            }
            catch (Exception ex)
            {
                Assert.Fail(ex.ToString());
            }
        }
        [Test]
        public void Delete()
        {
            try
            {
			    rt.fias.model.fias.AddressObjectType entityC = CreateNew();
				manager.Save(entityC);
				manager.Session.GetISession().Flush();
				manager.Session.GetISession().Clear();
			
                rt.fias.model.fias.AddressObjectType entity = GetFirstAddressObjectType();
				
                manager.Delete(entity);

                entity = manager.GetById(entity.Id);
                Assert.IsNull(entity);
            }
            catch (Exception ex)
            {
                Assert.Fail(ex.ToString());
            }
        }
	}
}

